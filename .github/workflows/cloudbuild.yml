    1 steps:
    2 # Step 1: Build your Nix derivation or run tests
    3 - name: 'nixos/nix'
    4   args: ['develop', '--command', 'nix-build', 'tests/default.nix']
    5
    6 # Step 2: Push a resulting container if applicable
    7 - name: 'gcr.io/cloud-builders/docker'
    8   args: ['build', '-t', 'gcr.io/$PROJECT_ID/my-nixos-app', '.']
    9
   10 images:
   11 - 'gcr.io/$PROJECT_ID/my-nixos-app'
